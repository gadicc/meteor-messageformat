CLI tool for Meteor MessageFormat

Since 0.0.4, a wrapper to call mf_extract.js from the current project.
Hence (in theory), no need to update this from npm again.

See http://messageformat.meteor.com/.
